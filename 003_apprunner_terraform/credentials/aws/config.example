[sso-session sso]
sso_start_url = # ssoのログインurlを設定する
sso_region = ap-northeast-1

# 適切にプロファイルを設定する
[profile aws-stg]
sso_session = sso
sso_account_id =
sso_role_name = AdministratorAccess
region = ap-northeast-1
output = json

# 適切にプロファイルを設定する
[profile aws-prod]
sso_session = sso
sso_account_id =
sso_role_name = AdministratorAccess
region = ap-northeast-1
output = json
