description: 401 Unauthorized.
content:
  application/json:
    schema:
      type: object
      required:
        - detail
      properties:
        detail:
          type: string
          example: "Unauthenticated."
