
post:
  tags:
    - accounts
  summary: ログイン
  description: description
  operationId: accounts_login
  requestBody:
    content:
      application/json:
        schema:
          $ref: "../requests/accounts_login.yml"
  responses:
    "200":
      description: 正常
      content:
        application/json:
          schema:
            $ref: "../responses/one_time_password.yml"
