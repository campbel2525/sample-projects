
post:
  tags:
    - accounts
  summary: メールアドレス更新
  description: description
  operationId: accounts_update_email
  security:
    - Authentication: []
  requestBody:
    content:
      application/json:
        schema:
          $ref: "../requests/accounts_update_email.yml"
  responses:
    "200":
      description: 正常
      content:
        application/json:
          schema:
            $ref: "../responses/one_time_password.yml"
