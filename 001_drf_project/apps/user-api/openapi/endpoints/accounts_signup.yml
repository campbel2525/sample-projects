
post:
  tags:
    - accounts
  summary: 新規登録
  description: description
  operationId: accounts_signup
  requestBody:
    content:
      application/json:
        schema:
          $ref: "../requests/accounts_signup.yml"
  responses:
    "200":
      description: 正常
      content:
        application/json:
          schema:
            $ref: "../responses/one_time_password.yml"
